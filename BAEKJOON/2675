#include <iostream>
using namespace std;

int main() {
	int t, r;
	string s;
	cin >> t;
	for (int i = 0; i < t; i++)
	{
		cin >> r >> s;
		for (int j = 0; j < s.size(); j++)
			for (int ii = 0; ii < r; ii++) cout << s[j];
		cout << endl;
	}
}
