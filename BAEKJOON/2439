#include <iostream>
using namespace std;

int main(){
    cin.tie(0);
    ios_base::sync_with_stdio(0);
    int n;
    cin >> n;
    for (int i = 1; i <= n; i++)
    {
        int a = n - i, b = i;
        while (a--) cout << ' ';
        while (b--) cout << '*';
        cout << '\n';
    }
}
