#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    cout.tie(NULL);
    ios_base::sync_with_stdio(false);

    int A, B, V;
    cin>>A>>B>>V;
    double C = (double)(V - A) / (A - B);
    cout << (int)ceil(C) + 1; // ceil() = 올림 함수
}
