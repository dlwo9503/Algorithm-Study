#include <iostream>
using namespace std;

int main() {
    int arr[4] = { 25,10,5,1 }, t, a;
    cin >> t;
    while (t--)
    {
        cin >> a;
        for (int i = 0; i < 4; i++)
        {
            cout << a / arr[i] << ' ';
            a %= arr[i];
        }
        cout << '\n';
    }
}
