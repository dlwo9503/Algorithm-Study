#include <iostream>
using namespace std;

int fibonaci(int n){
    if (n < 2) return n;
    else return fibonaci(n - 1) + fibonaci(n - 2);
}

int main(){
    int n;
    cin >> n;
    cout << fibonaci(n);
}
